/**
 *
 * @param {[]} A
 * @returns {number}
 */

function findOdd(A) {
  //happy coding!
  const setA = new Set(A);
  const mapA = new Map();
  console.log(mapA);
  return 0;
}
findOdd([1, 0, 1, 0, 1]);
module.exports = findOdd;
